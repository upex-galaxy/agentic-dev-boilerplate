{
  "name": "agentic-dev-boilerplate",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "scripts": {
    "setup": "bun cli/doctor.ts --preflight && bun cli/install.ts",
    "setup:doctor": "bun cli/doctor.ts",
    "up": "bun cli/update-boilerplate.ts",
    "onboarding": "bun scripts/onboarding.ts",
    "api:sync": "bun scripts/sync-openapi.ts",
    "jira:sync-issues": "bun run scripts/sync-jira-issues.ts",
    "vars:check": "bun run scripts/lint-vars.ts",
    "skills:check": "bun run scripts/lint-skills.ts",
    "skills:validate": "bun cli/install.ts --validate-skills",
    "jira:sync-fields": "bun run scripts/sync-jira-fields.ts",
    "jira:sync-workflows": "bun run scripts/sync-jira-workflows.ts",
    "jira:check": "bun run scripts/check-jira-setup.ts",
    "agents:setup": "bun run scripts/agents-setup.ts",
    "format:fix": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:check": "eslint .",
    "lint:fix": "eslint --fix .",
    "types:check": "tsc --noEmit",
    "repo:check": "bun run format:check && bun run lint:check && bun run types:check && bun run vars:check && bun run skills:check",
    "repo:fix": "bun run format:fix && bun run lint:fix && bun run types:check && bun run vars:check && bun run skills:check",
    "clean": "rm -rf node_modules dist",
    "prepare": "husky",
    "claude": "dotenv -e .env -- claude",
    "opencode": "dotenv -e .env -- opencode"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "@inquirer/prompts": "^8.1.0",
    "boxen": "^8.0.1",
    "cli-table3": "^0.6.5",
    "figures": "^6.1.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.16.0",
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.3",
    "dotenv-cli": "^8.0.0",
    "eslint": "^9.28.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
